# sim-storage

In one line: the places SIM keeps things -- lists and lookup tables, eager or lazy, layered or on disk.

## What it gives you

Ordered linked lists, lists that compute their contents only when asked, fast key-to-value lookups, a path-addressed folder store, tables whose values are worked out on first use and then remembered, and stacks of tables where upper layers can cover the entries beneath them.

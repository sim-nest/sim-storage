[package]
name = "sim-artifact-facet"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Pure role-safe artifact facet comparison and bounded three-way merge for SIM."
readme = "README.md"
publish = true
repository.workspace = true
homepage.workspace = true

[package.metadata.sim-recipes]
policy = "descriptor"
reason = "The recipe is a machine-indexed description of the pure facet law."

[dependencies]
sim-kernel = { version = "0.4.0" }

[lints]
workspace = true

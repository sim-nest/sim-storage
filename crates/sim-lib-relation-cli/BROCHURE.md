# Operate relational data without raw SQL

`sim-lib-relation-cli` makes sites, schemas, migrations, checked queries,
checked mutations, and mounts discoverable through one loadable command. Every
action names admitted artifacts, carries bounds, and requires explicit product
authorization before adoption or mutation.

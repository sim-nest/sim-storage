# sim-lib-relation-cli

The loadable `sim relation` command. It accepts only named, pre-checked
relational artifacts and bounded operations. There is deliberately no raw SQL
argument or native-path escape hatch.

This recipe records read-only content search over a filesystem table. The host
keeps the search inside the table root, applies an optional glob before reading
file contents, and caps the number of returned matches.

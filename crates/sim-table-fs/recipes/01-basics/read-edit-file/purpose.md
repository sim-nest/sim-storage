This recipe records the general filesystem table flow: open a host directory as
a table, write one string leaf, patch a precise text span with edit permission,
and read the same key back through the table surface.

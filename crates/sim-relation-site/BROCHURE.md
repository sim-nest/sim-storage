# Bounded relation sites

Run the same admitted logical plan against any loaded database provider without
letting SQL text, provider locators, unbounded results, or raw transaction state
escape into application code. Typed bindings, pushed rows, capability checks,
kernel effects, redacted receipts, and total rollback live at one durable seam.

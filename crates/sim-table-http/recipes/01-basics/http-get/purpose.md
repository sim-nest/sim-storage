This recipe records the shape of reading one HTTP resource through a table
directory: the table carries a base address, codec, timeout, and body limit, and
the caller asks for one key. The actual socket operation requires network
permission from the host.
